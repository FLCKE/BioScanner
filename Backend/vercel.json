{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/", "destination": "/api/index.js" }  // optionnel: la racine va vers ton API
  ],
  "functions": {
    "api/**/*.js": { "maxDuration": 60, "memory": 1024, "regions": ["fra1"] }
  }
}
